//another useful function is the parseInt function. It takes a string, and returns an integer.
//It's used to make sure you're dealing with integers and not strings for certain calculations and things like that.

function convertToInteger(placeHolder_the_teacher_often_uses_str) 
{
    return parseInt(placeHolder_the_teacher_often_uses_str);
}

console.log(convertToInteger("52.252"));